# Spinin runtime dependencies. Windows only.
# Install with:  py -3.13 -m pip install -r requirements.txt
PyQt6>=6.7
mido>=1.3
python-rtmidi>=1.5
pywin32>=306
psutil>=5.9
pycaw>=20240210
comtypes>=1.2
AppOpener>=1.7
