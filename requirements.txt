# Spinin runtime dependencies.
# Install with:  py -3.13 -m pip install -r requirements.txt   (Windows)
#                python3 -m pip install -r requirements.txt    (macOS)
#
# The markers matter: pip installs only the backend for the platform it is running on, so
# neither build drags in the other's native packages.

# Cross-platform
PyQt6>=6.7
mido>=1.3
python-rtmidi>=1.5
psutil>=5.9

# Windows backend
pywin32>=306; sys_platform == "win32"
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2; sys_platform == "win32"
AppOpener>=1.7; sys_platform == "win32"

# macOS backend. Quartz carries the event services used to send keystrokes and to watch for
# a shortcut being recorded; Cocoa carries NSWorkspace, which is how the app in front is
# identified. CoreAudio is reached through ctypes and needs no package.
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
